# Nixpacks configuration for Railway
# Build Node.js application only

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["NODE_ENV=development npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
