# Core Face Recognition Dependencies
deepface==0.0.79
tensorflow==2.16.1
opencv-python-headless==4.8.1.78

# Image Processing
Pillow==10.0.1
numpy==1.24.3

# Note: Using opencv-python-headless to avoid GUI library dependencies
# DeepFace already handles all face recognition needs